## Summary
- Implement checklist template system for apartment inspections (issue #10)
- JSON validation enforces strict schema: category, label, allowed_results, order (no extra keys)
- Auto-create ChecklistTemplate with 22 default items on apartment creation via post_save signal
- Each apartment gets an independent copy (deepcopy) for per-apartment customization
- Default items cached with lru_cache for performance

Closes #10

## Changes
- `apps/apartments/models.py` — `validate_checklist_items()` validator, `ChecklistTemplate` model
- `apps/apartments/signals.py` — post_save signal for auto-creating templates
- `apps/apartments/apps.py` — signal registration in AppConfig.ready()
- `apps/inspections/checklist_defaults.py` — cached fixture loader with error handling
- `apps/apartments/migrations/0002` — migration for items field
- `tests/apartments/test_checklist_template.py` — 32 tests across 4 test classes
- `tests/factories.py` — updated ChecklistTemplateFactory

## Validation
- [x] `make lint` passes
- [x] `make test` passes (166 tests)
- [x] `make manage CMD="check"` passes
- [x] Code review — addressed all Important issues (extra key rejection, lru_cache, order=0 test)

Co-Authored-By: Claude <noreply@anthropic.com>
