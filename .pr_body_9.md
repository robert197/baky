## Summary
- Custom storage module with photo validation (10MB limit, JPEG/PNG/HEIC), auto-thumbnail generation (300x300 max), HEIC-to-JPEG conversion, EXIF orientation handling, and signed URL support
- Production settings: S3Boto3Storage as default backend with querystring auth (24h expiry)
- Photo model: custom upload paths (UUID-based), file validators, auto-thumbnail and HEIC conversion in save()
- Added pillow-heif dependency for HEIC/HEIF support
- 43 new tests across storage utilities and Photo model integration

Closes #9

## Changes
- baky/storage.py — new storage utilities module
- baky/settings/production.py — S3/R2 backend + querystring auth config
- apps/inspections/models.py — Photo model with custom upload_to, validators, save() override
- requirements/base.txt — added pillow-heif
- Migration for Photo field changes

## Validation
- [x] make lint passes
- [x] make test passes (209 tests, all green)
- [x] make manage CMD=check passes
- [x] Code review — addressed thread-safety, EXIF orientation, file seek issues

Co-Authored-By: Claude <noreply@anthropic.com>
